====================================================================== [DESCRIPCION DE COMMITS! =======================================================================
#8
Creamos hilos para recibir peticiones.
IO: creamos hilos para manejar las peticiones del KERNEL
MEMORIA: creamos hilos para manejar como servidor la conexion con el CPU
CPU: creamos hilos para manejar mensajes/peticiones futuras del KERNEL y la MEMORIA
KERNEL: arreglados los comentarios y creados los hilos para manejar las conexiones con sus clientes 

#10
- Correccion de warnings
- Implementacion de argumentos para iniciar el modulo CPU e IO
    usar este comando: desde la carpeta del modulo que queramos inicializar desde la terminal de linux
    "./bin/{modulo} [argumentos]
- Creacion de archivos de Logs independientes de cada CPU segun su ID.

#21
- Implemento una descripcion de las funciones, strcuts, etc; en los Headers dentro del modulo de Kernel

#22
- Implemento una descripcion de las funciones, strcuts, etc; en los Headers dentro del modulo de IO
- Inicio el handshake entre IO y Kernel

========================================================================= [LISTA DE TODO!] =======================================================================
* Chequear si es necesario un hilo de escucha de peticiones de memoria.

. CPU:
    - Envio de peticion de instruccion a memoria con el pid y pc recibido del kernel.

. MEMORIA:
    - Definir estructuras para manejo de archivos de pseudocódigo/instrucciones
    - Traducir las direcciones lógicas (propias del proceso) a direcciones físicas (instrucciones que afecten a la memoria)
    - Definir qué se considera cómo una linea

. IO:
    - Descripcion de funciones
    - Generar issue: ¿Cómo definir las interfaces de IO? - Me fijo yo vini esto
    - Implementar la funcion que hizo luchito de inicializar config

. KERNEL:
    - Ver como hacer que el Kernel sepa desde antes el strlen de la interfaz que le es enviada

. GENERALES:
    - Chequear warnings de cada modulo. 
        UPDATE: Warnings generales corregidos, tener cuidado con los (void*), no se por que es motivo de warning





CHECKPOINT 2

    Módulo Kernel:
        - Planificación de corto y largo plazo con FIFO
        - Administración de IO

    Módulo CPU:
        - Interpretación de instrucciones y ejecución de instrucciones

    Módulo Memoria:
        - Devolver lista de instrucciones
        - Devolver un valor fijo de espacio libre (mock)

    Módulo IO: Completo 